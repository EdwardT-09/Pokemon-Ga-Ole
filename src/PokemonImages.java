import javax.swing.*;
import java.awt.*;

// Used to store the icons/images for the pokemon to be used in pokedex
public class PokemonImages {

    static ImageIcon poke1 = new ImageIcon("milotic.png"); // load the image to a imageIcon
    static Image poke1i = poke1.getImage(); // transform it
    static Image poke1ii = poke1i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon miloticIcon = new ImageIcon(poke1ii);  // transform it back

    static ImageIcon poke2 = new ImageIcon("squirtle.png"); // load the image to a imageIcon
    static Image poke2i = poke2.getImage(); // transform it
    static Image poke2ii = poke2i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon squirtleIcon = new ImageIcon(poke2ii);  // transform it back

    static ImageIcon poke3 = new ImageIcon("seel.png"); // load the image to a imageIcon
    static Image poke3i = poke3.getImage(); // transform it
    static Image poke3ii = poke3i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon seelIcon = new ImageIcon(poke3ii);  // transform it back

    static ImageIcon poke4 = new ImageIcon("gyarados.png"); // load the image to a imageIcon
    static Image poke4i = poke4.getImage(); // transform it
    static Image poke4ii = poke4i.getScaledInstance(165, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon gyaradosIcon = new ImageIcon(poke4ii);  // transform it back

    static ImageIcon poke5 = new ImageIcon("suicune.png"); // load the image to a imageIcon
    static Image poke5i = poke5.getImage(); // transform it
    static Image poke5ii = poke5i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon suicuneIcon = new ImageIcon(poke5ii);  // transform it back

    static ImageIcon poke6 = new ImageIcon("charizard.png"); // load the image to a imageIcon
    static Image poke6i = poke6.getImage(); // transform it
    static Image poke6ii = poke6i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon charizardIcon = new ImageIcon(poke6ii);  // transform it back

    static ImageIcon poke7 = new ImageIcon("fennekin.png"); // load the image to a imageIcon
    static Image poke7i = poke7.getImage(); // transform it
    static Image poke7ii = poke7i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon fennekinIcon = new ImageIcon(poke7ii);  // transform it back

    static ImageIcon poke8 = new ImageIcon("coalossal.png"); // load the image to a imageIcon
    static Image poke8i = poke8.getImage(); // transform it
    static Image poke8ii = poke8i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon coalossalIcon = new ImageIcon(poke8ii);  // transform it back

    static ImageIcon poke9 = new ImageIcon("volcanion.png"); // load the image to a imageIcon
    static Image poke9i = poke9.getImage(); // transform it
    static Image poke9ii = poke9i.getScaledInstance(165, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon volcanionIcon = new ImageIcon(poke9ii);  // transform it back

    static ImageIcon poke10 = new ImageIcon("flareon.png"); // load the image to a imageIcon
    static Image poke10i = poke10.getImage(); // transform it
    static Image poke10ii = poke10i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon flareonIcon = new ImageIcon(poke10ii);  // transform it back

    static ImageIcon poke11 = new ImageIcon("meganium.png"); // load the image to a imageIcon
    static Image poke11i = poke11.getImage(); // transform it
    static Image poke11ii = poke11i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon meganiumIcon = new ImageIcon(poke11ii);  // transform it back

    static ImageIcon poke12 = new ImageIcon("sceptile.png"); // load the image to a imageIcon
    static Image poke12i = poke12.getImage(); // transform it
    static Image poke12ii = poke12i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon sceptileIcon = new ImageIcon(poke12ii);  // transform it back

    static ImageIcon poke13 = new ImageIcon("cherubi.png"); // load the image to a imageIcon
    static Image poke13i = poke13.getImage(); // transform it
    static Image poke13ii = poke13i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon cherubiIcon = new ImageIcon(poke13ii);  // transform it back

    static ImageIcon poke14 = new ImageIcon("snivy.png"); // load the image to a imageIcon
    static Image poke14i = poke14.getImage(); // transform it
    static Image poke14ii = poke14i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon snivyIcon = new ImageIcon(poke14ii);  // transform it back

    static ImageIcon poke15 = new ImageIcon("shaymin.png"); // load the image to a imageIcon
    static Image poke15i = poke15.getImage(); // transform it
    static Image poke15ii = poke15i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon shayminIcon = new ImageIcon(poke15ii);  // transform it back

    static ImageIcon poke16 = new ImageIcon("Ho-Oh.png"); // load the image to a imageIcon
    static Image poke16i = poke16.getImage(); // transform it
    static Image poke16ii = poke16i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon ho_ohIcon = new ImageIcon(poke16ii);  // transform it back

    static ImageIcon poke17 = new ImageIcon("talonflame.png"); // load the image to a imageIcon
    static Image poke17i = poke17.getImage(); // transform it
    static Image poke17ii = poke17i.getScaledInstance(165, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon talonflameIcon = new ImageIcon(poke17ii);  // transform it back

    static ImageIcon poke18 = new ImageIcon("Rowlet.png"); // load the image to a imageIcon
    static Image poke18i = poke18.getImage(); // transform it
    static Image poke18ii = poke18i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon rowletIcon = new ImageIcon(poke18ii);  // transform it back

    static ImageIcon poke19 = new ImageIcon("Skiploom.png"); // load the image to a imageIcon
    static Image poke19i = poke19.getImage(); // transform it
    static Image poke19ii = poke19i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon skiploomIcon = new ImageIcon(poke19ii);  // transform it back

    static ImageIcon poke20 = new ImageIcon("peliper.png"); // load the image to a imageIcon
    static Image poke20i = poke20.getImage(); // transform it
    static Image poke20ii = poke20i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon peliperIcon = new ImageIcon(poke20ii);  // transform it back

    static ImageIcon poke21 = new ImageIcon("sudowoodo.png"); // load the image to a imageIcon
    static Image poke21i = poke21.getImage(); // transform it
    static Image poke21ii = poke21i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon sudowoodoIcon = new ImageIcon(poke21ii);  // transform it back

    static ImageIcon poke22 = new ImageIcon("lileep.png"); // load the image to a imageIcon
    static Image poke22i = poke22.getImage(); // transform it
    static Image poke22ii = poke22i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon lileepIcon = new ImageIcon(poke22ii);  // transform it back

    static ImageIcon poke23 = new ImageIcon("bonsly.png"); // load the image to a imageIcon
    static Image poke23i = poke23.getImage(); // transform it
    static Image poke23ii = poke23i.getScaledInstance(150, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon bonslyIcon = new ImageIcon(poke23ii);  // transform it back

    static ImageIcon poke24 = new ImageIcon("archeops.png"); // load the image to a imageIcon
    static Image poke24i = poke24.getImage(); // transform it
    static Image poke24ii = poke24i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon archeopsIcon = new ImageIcon(poke24ii);  // transform it back

    static ImageIcon poke25 = new ImageIcon("regirock.png"); // load the image to a imageIcon
    static Image poke25i = poke25.getImage(); // transform it
    static Image poke25ii = poke25i.getScaledInstance(180, 120,  java.awt.Image.SCALE_SMOOTH); // scale it the smooth way
    public static ImageIcon regirockIcon = new ImageIcon(poke25ii);  // transform it back
}